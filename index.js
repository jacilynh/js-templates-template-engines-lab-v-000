// Must have a form with an id of post-form

// index, functions, createPost, builds the proper templates

// index, functions, postComment, builds the proper templates

// index, templates, has a comments template: Must have a lodash template with an id of 'comments-template'

// index, templates, has a comment template: Must have a lodash template with an id of 'comment-template'

// index, templates, has a page template: Must have a lodash template with an id of 'page-template'

// index, templates, has a post template: Must have a lodash template with an id of 'post-template'